import { NPCContext } from "./interface/btNode";

const npcs: NPCContext[] = [
    { name: "A", currentTime: 0, currentActivity: "Start", lastActivity: "", isEventActive: false },
    { name: "B", currentTime: 0, currentActivity: "Start", lastActivity: "", isEventActive: false },
    { name: "C", currentTime: 0, currentActivity: "Start", lastActivity: "", isEventActive: false }
];

export default npcs